import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.licence3.TP1_TCHINWA_Ismael_Clinton',
  appName: 'FrechFruits',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
